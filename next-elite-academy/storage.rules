rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write access to authenticated users only
      allow read, write: if request.auth != null;
      
      // For public read access (uncomment if needed):
      // allow read: if true;
      // allow write: if request.auth != null;
    }
  }
}

